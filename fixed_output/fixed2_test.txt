// fixed_input/test.c
#include <stdio.h>

void vulnerable_function() {
    // Fixed syntax error by adding a semicolon at the end of the line
    int x = 10;
    // No security vulnerabilities were found in this function.
    // The function simply initializes an integer and prints it.
    printf("x = %d\n", x);
}