#include <stdio.h>
#include <string.h>

// Corrected vulnerable_function to prevent buffer overflow
void vulnerable_function(char *input) {
    char buffer[10];
    // Use strncpy to safely copy input to buffer, ensuring no overflow
    strncpy(buffer, input, sizeof(buffer) - 1);
    // Null-terminate the buffer to prevent any potential overflow issues
    buffer[sizeof(buffer) - 1] = '\0';
    printf("Buffer: %s\n", buffer);
}