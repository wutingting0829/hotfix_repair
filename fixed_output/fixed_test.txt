// fixed_input/test.c
#include <stdio.h>

void vulnerable_function() {
    // Fixed missing semicolon in the declaration of x
    int x = 10;
    // Added a format specifier to ensure the correct type is printed
    printf("x = %d\n", x);
}